{
  "name": "functions",
  "description": "Cloud Functions for Firebase",
  "scripts": {
    "lint": "eslint .",
    "build": "tsc",
    "serve": "npm run build && npx firebase-tools emulators:start --only functions",
    "shell": "npm run build && npx firebase-tools functions:shell",
    "start": "npm run shell",
    "deploy": "npm run build && npx firebase-tools deploy --only functions",
    "logs": "npx firebase-tools functions:log",
    "test": "npm run build && npx firebase-tools emulators:exec --only firestore \"mocha --require ts-node/register --timeout 15000 test/**/*.test.ts\""
  },
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "dependencies": {
    "@sentry/integrations": "^7.114.0",
    "@sentry/node": "^10.26.0",
    "cors": "^2.8.5",
    "expo-server-sdk": "^5.0.0",
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^7.0.0",
    "jsonwebtoken": "^9.0.3"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "@types/chai": "^5.2.2",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/mocha": "^10.0.10",
    "@types/sinon": "^17.0.4",
    "@typescript-eslint/eslint-plugin": "^8.56.0",
    "@typescript-eslint/parser": "^8.56.0",
    "chai": "^5.2.0",
    "eslint": "^9.39.2",
    "eslint-plugin-import": "^2.25.4",
    "firebase": "^12.11.0",
    "firebase-functions-test": "^3.4.1",
    "mocha": "^11.7.0",
    "sinon": "^21.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.0"
  },
  "private": true
}
