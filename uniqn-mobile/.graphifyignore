# graphify 지식그래프 제외 목록
#
# 이 그래프의 역할은 "코드·SQL 구조" 전용이다. 문서 발견은 옵시디언 색인
# SessionStart 훅이 이미 담당하므로 여기서 문서를 다시 넣으면 중복이 된다.
#
# 게다가 실해가 있다: md 헤딩이 노드로 들어가면 한글 질의가 문서 서브트리에
# 갇혀 코드를 못 찾는다(실측 — "출퇴근 체크인은 어디서 처리되나" 질의가
# 코드 0건, 문서 헤딩 10건 반환). 엣지의 2.5%만 잃고 질의 품질을 되찾는다.
*.md

# squash된 과거 마이그레이션. 라이브 스키마로 오독되면 이미 고쳐진 버그가
# 되살아난다(실측 — 20260620151331 이 제거한 중복 트리거가 재검출됨).
supabase/migrations/archive/
