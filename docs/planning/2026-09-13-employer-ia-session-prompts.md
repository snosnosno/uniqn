# 구인자 IA 재설계 — 실행 세션 프롬프트

- 작성일: 2026-09-13
- 상태: 설계 합의 완료 · 구현 미착수
- 목업: 아티팩트 v4 (사용자 보유 링크) · 팔레트는 `src/constants/colors.ts` 실제 토큰
- 선행 분석: [관계·중복](2026-09-12-schedule-team-posting-ia-analysis.md) · [워크플로우](2026-09-12-employer-workflow-ux-analysis.md) · [PRD 위치](2026-09-12-prd-where-schedule-and-team-belong.md)
- 기준선: [근무표 제품 기획 의도](2026-09-11-work-schedule-product-intent.md)

---

## 0. 합의된 결론 (재논의 금지)

| | 결정 | 근거 |
|---|---|---|
| 공고 상세 진입점 | **12개 → 3개** (`지원자` · `근무` · `공고 수정`) | 매일 누르는 건 둘뿐 |
| 지원자 ↔ 근무 | **합치지 않는다** | 축이 다르다 — 지원=공고 단위 1줄, 근무=날짜 단위 N줄 |
| 정산 | **워크플로우 삭제. 금액 계산·표시만** | 앱이 돈을 만지지 않는데 버튼이 잠기는 상태였다 |
| 팀 | **남긴다** | 없애지 않고, 협업자와의 범위 차이를 화면에 한 줄로 |
| 내 스케줄 탭(구직자) | **구조는 건드리지 않는다** | 단, 정산 라벨 정리는 불가피(§3) |
| 하단 탭 | **5개 유지** | "오늘"은 탭이 아니라 있을 때만 뜨는 한 줄 |
| 근무표 | 버튼 → **[공고]/[근무표] 세그먼트** | 세그먼트 왼쪽이 공고라 신규 사용자 경험은 그대로 |

### 되돌리면 안 되는 것 (실사고 근거 있음)

- **진입점을 신호에 따라 숨기지 않는다.** "지금 할 일"로 승격돼도 목록에서 빼지 않는 규칙이
  `app/(employer)/my-postings/[id]/index.tsx` 주석에 사고 기록과 함께 남아 있다.
  자리가 움직이면 사장은 "메뉴가 없어졌다"고 읽는다.
- **조건부 숨김은 "0개일 때"만.** 있다가 없어지는 건 금지.
- 이번 웨이브에서 되돌린 #488 변경(근무표 헤더 아이콘)을 다시 하지 말 것.

---

## 1. 실측 현황 (2026-09-13 기준, 이 수치로 계획할 것)

```
payrollStatus / payroll_status 참조   133 파일  (소스 82 · 테스트 51)
정산 관련 RPC                          13개
구직자 쪽 정산 파일                     4개
  src/components/schedule/ScheduleCard.tsx
  src/components/schedule/tabs/InfoTab.tsx
  src/components/schedule/tabs/SettlementTab.tsx
  src/hooks/useSettlement.ts
정산 pgTAP                             settlement_custom_rpc / settlement_payroll_status_rpc /
                                       settlement_settle_rpcs / worklog_settled_no_show_lock 등
```

⚠️ **컬럼·RPC·트리거는 이번에 지우지 않는다.** 82개 소스 파일을 한 번에 흔드는 위험이
남겨두는 비용보다 훨씬 크다. UI에서 쓰지 않는 것까지가 1단계다.

🔑 **부수 효과**: 아무도 `지급 완료`를 누르지 않으면 `protect_work_log_payroll_columns()`의
완료 잠금이 발동하지 않아 **퇴근시간 정정이 항상 열린다.** 잠금을 제거하지 않고도
"이미 준 뒤 금액이 바뀜" 문제가 풀린다.

---

## 2. 세션 분할 — 순서 엄수

앞 세션이 끝나야 다음이 성립한다. 한 세션에 두 개를 묶지 말 것.

### S1. 공고 상세 진입점 12 → 3

**목표**: 공고 상세에 `지원자` · `근무` · `공고 수정` 세 타일과 `공유` · `⋯`만 남는다.

- 타일에서 제거 → 흡수처
  - `취소 요청 관리` → [근무] 줄의 상태 + 승인/거절/전화 버튼
  - `스태프 관리/정산` → **[근무]로 개명**하고 날짜별 그룹 유지
  - `스태프 공지` → [근무] 헤더의 `메시지`
  - `지원 QR` → `공유` 시트 안으로 (링크 · QR · 구직자 화면 보기)
  - `함께 관리할 사람` → `⋯` 안으로
  - `미리보기`(헤더 눈 아이콘) → 삭제. 공고 작성 마지막 단계로 옮긴다
  - `라이브 운영` → **그대로 둔다** (대회일 때만 뜨는 조건부라 평소엔 없다)
- 배지는 **합치지 않는다**. `3명 대기` / `취소요청 1` 각각 유지 —
  `할 일 6` 처럼 뭉치면 눌러봐야 안다
- 이름은 대상 기준으로: `스태프 관리/정산` → `근무`. "관리"로 끝나는 타일이 3개라 구분이 안 됐다

**검증**: `app/(employer)/my-postings/__tests__/` 의 기존 스위트 + 진입점 개수 단언 신설.
`npm run quality` · 영향권 jest.

### S2. 정산 워크플로우 제거 (UI 계층)

**목표**: 구인자 화면에서 `지급 대기 / 지급 완료` 상태가 사라지고 **금액만** 보인다.

- [근무] 화면
  - 퇴근이 찍힌 줄에만 금액을 붙인다. **안 찍힌 줄에 추정 금액을 만들지 않는다** —
    `퇴근을 찍어야 금액이 정해져요`
  - 맨 위 한 줄 `지급 예정 합계 474,000원 ›` → 누르면 **사람별 합산** 시트
    (민수의 3일치가 한 줄이어야 세 번 보내지 않는다)
  - `계산 근거` 시트: 출근·퇴근·인정 근무·시급·합계 + **변경 이력**
- 제거: 공고별 정산 화면(`my-postings/[id]/settlements.tsx`의 정산 탭) ·
  지점 정산 화면(`venue-settlements.tsx`) · 일괄 지급 · 지급 완료 버튼
- **구직자 쪽(§3)**: 라벨만 정리. 구조는 건드리지 않는다
- 컬럼·RPC·트리거·pgTAP는 **손대지 않는다**

**검증**: 금액 계산 결과가 기존 정산 화면과 동일한지 회귀 테스트.
구직자 화면에 `지급 대기` 문구가 남지 않는지 Grep(`e2e/`도 별도 확인 — eslint 사각지대).

### S3. 내 공고 탭 세그먼트 + 오늘 한 줄

**목표**: 탭 안에서 `[공고] / [근무표]` 전환. 오늘 신호는 **있을 때만** 맨 위 한 줄.

- 세그먼트 기본값은 **[공고]** — 신규 사용자 경험을 바꾸지 않는다
- 오늘 스트립: 미출근 · 퇴근 미기록 (0건이면 렌더하지 않음).
  누르면 오늘 상세로. 데이터는 기존 셀렉터 재사용 —
  `TodayOpsStrip` 계열 · `summarizeMissingCheckouts` · `useVenueMissingCheckouts` ·
  `selectPostingCapacityGaps`
- ⋯ 로 이동: 묶음 공유 · 받은 초대
- 팀 링크: **멤버 2명 이상일 때만** 노출

**검증**: 세그먼트 전환 + 오늘 스트립 0건 미렌더 단언.

### S4. 근무표 밀도 정리 + 출처 표시

- 사람 줄에 **출처 공고 칩** (`토요일 딜러 4명 공고에서`) — 컨테이너 직속 배치는 `직접 배치`
  🔑 이게 없어서 "공고는 근무표를 채우는 도구"가 화면에서 성립하지 않았다
- 칩 3개 + 입력칸 + 저장 버튼 → `3/5명 · 2명 부족` 한 줄, 숫자 탭하면 편집
  ⚠️ **PR #490에서 고친 수동/파생 분리를 깨지 말 것** — 입력칸은 `manualTarget`만 다룬다
- 지점·팀 선택기: 2개 이상일 때만
- `GridBadgeLegend` 제거 검토 (뱃지를 자명하게)

### S5. 팀·협업자 구분 문구

- 팀 화면 상단: `이 팀의 모든 공고를 함께 봅니다`
- 협업자 화면 상단: `이 공고 하나만 함께 봅니다` + 팀으로 가는 링크
- 멤버가 본인뿐이면 팀 진입점 미노출

---

## 3. 내 스케줄 탭(구직자)에서 불가피한 변경

사용자 지시는 **"내 스케줄 탭은 건드리지 말자"**였고 구조는 지킨다.
다만 정산 상태를 없애면 구직자가 영원히 `지급 대기`를 보게 되므로 **라벨만** 고친다.

| 파일 | 지금 | 바꿀 것 |
|---|---|---|
| `src/components/schedule/tabs/SettlementTab.tsx` | 지급 대기 / 지급 완료 배지 | 금액 + 변경 이력 |
| `src/components/schedule/ScheduleCard.tsx` | 정산 상태 표시 | 금액만 |
| `src/components/schedule/tabs/InfoTab.tsx` | 정산 상태 참조 | 동일 |
| `src/hooks/useSettlement.ts` | 상태 변이 훅 | 읽기만 쓰도록 호출부 정리 |

**구직자 문구 원칙**: `입금은 사장님이 직접 보냅니다` · `금액이 다르면 사장님께 문의하세요`.
앱이 지급을 보증하는 것처럼 읽히면 안 된다.

---

## 4. 아직 안 정해진 것 (S1 착수 전에 사용자에게 물을 것)

1. **상시(fixed) 공고 상세** — 날짜가 없어 출퇴근·정산이 아예 없다. 타일 3개 중 [근무]가 빈다.
   → "이 공고는 사람을 모으는 용도예요. 실제 근무일은 근무표에서 배치하세요" + 근무표 링크로 충분한가?
2. **대회 하나에 공고가 몇 개인가** — 딜러·플로어를 따로 올리는지, 하나로 올리는지.
   묶음 단위(대회 = 지점인가 공고인가)가 여기서 갈린다.
3. **세그먼트 [공고]/[근무표]의 범위 충돌** — 공고 목록은 팀 전체, 근무표는 지점 하나다.
   지점이 2곳이면 근무표 안에서 또 골라야 한다. 축을 "나"로 바꾸고 지점을 필터로 내릴 것인가?
4. **출퇴근 QR 지점당 1장** — ⚠️ UI 변경이 아니다. 페이로드가
   `{type:'venue', jobPostingId}`(`src/services/work/eventQRService.ts:175`)라
   서버가 "이 사람의 오늘 근무"를 찾아야 한다. 별도 웨이브로 뺄 것인가?

---

## 5. 세션 시작 체크리스트

```bash
# 1. 병렬 세션 확인 — 이 프로젝트는 워크트리가 상시 여럿이다
git worktree list && git status --short

# 2. 전용 워크트리 + 브랜치 (메인 체크아웃에서 구현 금지)
git fetch origin
git worktree add ../T-HOLDEM-wt-<작업명> -b feat/employer-ia-s<N> origin/master
cmd //c "mklink /J <워크트리>\uniqn-mobile\node_modules <메인>\uniqn-mobile\node_modules"
# ⚠️ 워크트리에서 npm install 금지 — node_modules 가 정션이라 다른 워크트리까지 바뀐다

# 3. 완료 전
npm run quality          # tsc + eslint + prettier + css-vars + rpc 대조
npx jest --testPathPattern="<영향권>"
# ⚠️ e2e/ 는 eslint ignores 라 상수·문구 변경 시 별도 Grep 필수
```

---

## 6. 이 웨이브의 선행 작업 (완료)

PR **#490** — 근무표 진입점 복원 + P0 2건

- `#488`에서 헤더 아이콘으로 옮겼던 근무표 진입점을 본문 버튼으로 복원
- **P0-1**: 근무표 목표 인원 입력칸이 공고 파생값(`max(수동, 좌석합)`)을 프리필해
  저장 한 번에 수동 목표를 덮던 문제 → `GridDayCell`에 `manualTarget`/`derivedRequired` 분리
- **P0-2**: 지점 2개 이상에서 칩 미선택 발행 시 `venue_id` 없이 저장돼
  근무표에서 조용히 사라지던 문제 → 제출 차단 + 사유 안내

검증: `npm run quality` exit 0 · 영향권 135스위트 1,192건 pass · P0-1 red-green 확인.
