# T-HOLDEM LLM Wiki Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** T-HOLDEM 프로젝트 지식을 LLM이 유지하는 `wiki/` 합성 레이어로 부트스트랩한다(골격 + 백본 시드 + 슬래시 커맨드 + 최신화 감지).

**Architecture:** 3레이어(원천소스=코드/docs/memory 읽기전용 / `wiki/`=LLM 소유 합성 / `wiki/AGENTS.md`=스키마). 얕은 카테고리(architecture·decisions·domain·sources) + `[[링크]]` 중심. 운영은 `/ingest`·`/query`·`/lint` 슬래시 커맨드(얇은 진입점, 규칙 단일진실원=AGENTS.md). 최신화는 결정적 스크립트로 자동 감지, 갱신은 사람이 트리거.

**Tech Stack:** Markdown + YAML frontmatter, Obsidian(기존 루트 `.obsidian` 볼트), bash(감지 스크립트), git.

**Spec:** `docs/superpowers/specs/2026-06-18-llm-wiki-design.md`

## Global Constraints

- 본문 한글, 코드 참조는 `파일:줄` 형식.
- frontmatter 의무 5필드: `area`(architecture|decisions|domain|sources) · `updated`(YYYY-MM-DD) · `status`(current|stale|draft) · `sources`(YAML 리스트) · `tags`(YAML 리스트).
- 무출처 주장 금지. "코드로 검증됨" vs "주장" 구분 표기.
- 원천 소스(코드 `uniqn-mobile/`, `docs/`, `memory/`) 절대 수정 금지 — 위키는 읽기만.
- 페이지 ≤200줄. 넘으면 분리.
- cross-link ≥3개(그중 타 영역 ≥1개). dangling 링크 허용(TODO 표식).
- 슬래시 커맨드·스크립트는 얇은 진입점. 규칙·워크플로우 단일진실원 = `wiki/AGENTS.md`(중복 금지).
- staleness 스크립트가 추적하는 소스 = **repo-relative 파일 경로**(코드/docs). `PR#`·SHA·`memory/`(repo 밖)는 정보성 소스로 staleness 비추적.
- 오늘 날짜: 2026-06-18.
- 작업 브랜치: `feat/llm-wiki` (이미 생성됨, 여기에 계속 커밋).
- 커밋 메시지: `<type>(wiki): <한글>` + `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`.

---

## File Structure

생성 파일:
- `wiki/AGENTS.md` — 스키마(규약+워크플로우). 위키의 심장.
- `wiki/index.md` — 내용 카탈로그(카테고리별 + 한줄요약).
- `wiki/log.md` — 시간순 append-only 로그.
- `wiki/overview.md` — "여기서 시작" 합성 + 허브 링크.
- `wiki/architecture/{layers,data-flow,rls-model}.md` — 아키텍처 백본 시드.
- `wiki/domain/{roles,target-market,revenue-model}.md` — 도메인 백본 시드.
- `wiki/decisions/{enum-divergence,worktime-ssot,capacity-full}.md` — 결정 시드(pitfall 합성, 최소 3건).
- `wiki/scripts/check-staleness.sh` — stale 페이지 감지(결정적).
- `wiki/scripts/check-unprocessed-docs.sh` — 미흡수 docs 감지.
- `.claude/commands/{ingest,query,lint}.md` — 슬래시 커맨드 3종.

수정 파일:
- `CLAUDE.md` — 루트에 위키 포인터 1줄 추가(기존 내용 보존).

---

## Task 1: 디렉토리 스캐폴드 + 골격 파일 + CLAUDE.md 포인터

**Files:**
- Create: `wiki/index.md`, `wiki/log.md`, `wiki/overview.md`
- Create dirs: `wiki/architecture/`, `wiki/decisions/`, `wiki/domain/`, `wiki/sources/`, `wiki/scripts/`
- Modify: `CLAUDE.md` (포인터 1줄 추가)

**Interfaces:**
- Produces: 위키 디렉토리 골격 + index/log/overview 스켈레톤. 후속 태스크가 여기에 페이지·스크립트·커맨드를 채운다.

- [ ] **Step 1: 디렉토리 생성**

```bash
cd "/c/Users/user/Desktop/T-HOLDEM"
mkdir -p wiki/architecture wiki/decisions wiki/domain wiki/sources wiki/scripts
```

- [ ] **Step 2: `wiki/log.md` 작성**

```markdown
# Wiki Log

> 시간순 append-only. 포맷: `## [YYYY-MM-DD] <op> | <제목>` (op = bootstrap | ingest | query | lint)
> 최근 5건: `grep "^## \[" wiki/log.md | tail -5`

## [2026-06-18] bootstrap | 위키 초기화 (골격 + 백본 시드 + 커맨드 + 감지 스크립트)
```

- [ ] **Step 3: `wiki/index.md` 작성** (스켈레톤 — Task 8에서 채움)

```markdown
# Wiki Index

> 전 위키 페이지 카탈로그. 질문 답변 시 여기부터 읽고 페이지로 드릴다운.
> 규약: [[AGENTS]] · 시작점: [[overview]]

## architecture
_(Task 5에서 채움)_

## decisions
_(Task 7에서 채움)_

## domain
_(Task 6에서 채움)_

## sources
_(ingest로 점진 추가)_
```

- [ ] **Step 4: `wiki/overview.md` 작성** (스켈레톤 — Task 8에서 합성)

```markdown
---
area: architecture
updated: 2026-06-18
status: draft
sources: []
tags: [overview, hub]
---

# T-HOLDEM 위키 — 여기서 시작

홀덤 스태프 관리앱(UNIQN) 프로젝트 지식 위키. 규약은 [[AGENTS]] 참조.

## 허브
_(Task 8에서 백본 페이지 링크로 채움)_
```

- [ ] **Step 5: `CLAUDE.md`에 포인터 1줄 추가**

`CLAUDE.md`의 `## 아키텍처` 섹션 바로 위(또는 `## 핵심 규칙` 표 다음)에 한 줄 추가. 기존 내용은 절대 수정하지 않고 추가만:

```markdown
## 지식 위키
프로젝트 지식 합성 레이어는 `wiki/`. 위키 작업(ingest/query/lint) 시 `wiki/AGENTS.md` 규약 준수. 운영: `/ingest` `/query` `/lint`.
```

- [ ] **Step 6: 검증**

Run: `ls -R wiki && grep -n "지식 위키" CLAUDE.md`
Expected: 5개 디렉토리 + index/log/overview 존재, CLAUDE.md에 포인터 라인 존재.

- [ ] **Step 7: Commit**

```bash
git add wiki/ CLAUDE.md
git commit -m "feat(wiki): 디렉토리 골격 + index/log/overview 스켈레톤 + CLAUDE.md 포인터

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 2: `wiki/AGENTS.md` 스키마 (위키의 심장)

**Files:**
- Create: `wiki/AGENTS.md`

**Interfaces:**
- Produces: 모든 운영(ingest/query/lint)·페이지 규약·증거 규칙의 단일 진실원. 슬래시 커맨드(Task 4)와 스크립트(Task 3)가 이 문서를 참조한다.

- [ ] **Step 1: `wiki/AGENTS.md` 작성** (아래 내용 그대로)

````markdown
# Wiki 스키마 (AGENTS.md)

이 문서는 LLM을 **규율 있는 위키 관리자**로 만드는 단일 진실원이다. 슬래시 커맨드(`/ingest` `/query` `/lint`)와 스크립트는 얇은 진입점이며, 실제 규칙은 모두 여기 있다.

## 1. 정체성
홀덤 스태프 관리앱(UNIQN) 프로젝트 지식 위키. 4영역:
- **architecture** — 시스템이 실제로 어떻게 도는가(레이어·데이터흐름·RLS·모듈).
- **decisions** — 왜 이렇게 됐는가(ADR + pitfall 합성).
- **domain** — 제품/비즈니스 지식(역할·타깃시장·수익모델·약관).
- **sources** — 개별 원천 소스 요약(ingest 1건당 1페이지).

## 2. 3레이어 + 불변식
- **원천 소스(불변)**: 코드 `uniqn-mobile/`, `docs/`, `memory/`, 깃 히스토리/PR. **절대 수정 금지.** 읽기만.
- **위키(`wiki/`)**: LLM이 소유·유지하는 합성 레이어. 여기만 쓴다.
- **스키마(`wiki/AGENTS.md`)**: 본 문서.

## 3. 페이지 규약
- 작고 단일 목적. 엔티티/개념/결정 하나당 파일 하나. ≤200줄(넘으면 분리).
- 파일명 kebab-case 슬러그. 영역 폴더 아래 배치.
- frontmatter 의무 5필드:
  ```yaml
  ---
  area: architecture        # architecture | decisions | domain | sources
  updated: 2026-06-18        # 최종 반영일 YYYY-MM-DD
  status: current            # current | stale | draft
  sources:                   # 근거 (repo 파일경로 / PR#NNN / memory/xxx)
    - uniqn-mobile/src/services/wallet/walletService.ts
    - PR#172
  tags: [wallet, payment]
  ---
  ```
- `[[위키링크]]` 적극. 페이지당 cross-link ≥3개(그중 타 영역 ≥1개). dangling 링크는 "쓸 거리" TODO로 허용.
- 본문 한글, 코드 참조 `파일:줄`.

## 4. 증거 규칙
- 모든 주장은 인용: `파일:줄` · `PR#NNN` · 커밋 SHA · `[[sources/...]]`.
- **"코드로 검증됨"**(실제 파일/줄 확인)과 **"주장"**(미검증 추정)을 구분 표기.
- 무출처 주장 금지.

## 5. Ingest 워크플로우 (`/ingest`)
1. 소스 + 본 스키마 읽기.
2. 핵심 takeaway를 사용자와 1~2줄 논의.
3. `wiki/sources/<slug>.md` 소스 요약 작성(압축률 10~20%, 직접 인용 ≤3줄).
4. 영향받는 architecture/decisions/domain 페이지 갱신(없으면 생성). 기존 주장과 **모순 시 플래그**.
5. `wiki/index.md` 해당 영역에 1줄 추가/갱신.
6. `wiki/log.md`에 `## [YYYY-MM-DD] ingest | <제목>` append.
- 제약: raw 수정 금지, frontmatter 누락 금지, 무출처 금지.

## 6. Query 워크플로우 (`/query`)
1. `wiki/index.md` 먼저 → `grep "^## \[" wiki/log.md | tail -5` → 본 스키마.
2. 관련 페이지 ≤7개로 좁힘(백링크 밀도·최신성·영역 매칭). 백링크 1단계 확장 허용.
3. 인용된 답변 합성(주장마다 `[[링크]]`). 데이터 갭은 명시.
4. 가치 있는 답변은 새 페이지로 환류 제안.
- 제약: raw 직접 읽기 지양(부족 시 명시 후 ≤2개), 읽기 전용(위키 안 씀), 추측 금지.

## 7. Lint 워크플로우 (`/lint`)
점검(자동 수정 X, 진단만):
- **stale 페이지**: `bash wiki/scripts/check-staleness.sh` (소스가 `updated` 이후 변경).
- **미흡수 docs**: `bash wiki/scripts/check-unprocessed-docs.sh`.
- **모순**: 페이지 간 충돌 주장.
- **고아**: 백링크 ≤2개.
- **데이터 갭**: 언급됐으나 자체 페이지 없는 개념.
- **증거 공백**: 무출처 주장.
출력: 진단 표 + 우선조치 top3 + 추가 탐색 제안. `log.md`에 lint 엔트리 append.
- 조치(갱신)는 사용자 승인 후 `/ingest` 또는 수동 편집.

## 8. 압축 룰
| 항목 | 룰 |
|---|---|
| 압축률 | 원천 1000줄 → 위키 100~200줄 (10~20%) |
| 페이지 길이 | ≤200줄 — 넘으면 분리 |
| 직접 인용 | 1소스당 ≤3줄 |
| cross-link | ≥3개 (그중 타 영역 ≥1개) |

## 9. memory/·docs/와의 관계
- `memory/`(repo 밖, ~/.claude/...) = LLM 세션 운영 메모. 위키 아님. 위키의 정보성 원천 소스로 읽힘(staleness 비추적).
- `docs/`(repo 내, 14 카테고리) = 사람/레거시 문서. 읽기 전용 원천. 이동/삭제 금지, `/ingest`로 점진 흡수.
- `wiki/` = 합성 레이어("X가 어떻게/왜 도는가"의 답).
````

- [ ] **Step 2: 검증**

Run: `grep -cE "^## " wiki/AGENTS.md`
Expected: 9 (섹션 1~9 모두 존재).

- [ ] **Step 3: Commit**

```bash
git add wiki/AGENTS.md
git commit -m "feat(wiki): AGENTS.md 스키마 — 규약·워크플로우 단일 진실원

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 3: 최신화 감지 스크립트 2종 (TDD Red-Green)

**Files:**
- Create: `wiki/scripts/check-staleness.sh`, `wiki/scripts/check-unprocessed-docs.sh`
- Test: 임시 fixture 페이지로 Red-Green 수동 검증

**Interfaces:**
- Produces: `check-staleness.sh`(소스 변경 감지), `check-unprocessed-docs.sh`(미흡수 docs). `/lint`(Task 4)가 호출.

- [ ] **Step 1: `wiki/scripts/check-staleness.sh` 작성**

```bash
#!/usr/bin/env bash
# 위키 페이지 stale 감지: repo 파일 소스가 페이지 updated 이후 변경됐는지 (결정적, LLM 불필요)
# PR#/SHA/memory(=repo 밖)는 정보성 소스로 비추적.
set -euo pipefail
REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
cd "$REPO_ROOT"
stale_count=0

mapfile -t pages < <(find wiki -name '*.md' \
  ! -name index.md ! -name log.md ! -name overview.md ! -name AGENTS.md)

for page in "${pages[@]}"; do
  fm="$(awk 'NR==1 && $0=="---"{f=1; next} f && $0=="---"{exit} f{print}' "$page")"
  [ -z "$fm" ] && continue
  updated="$(printf '%s\n' "$fm" | sed -n 's/^updated:[[:space:]]*//p' | head -1)"
  [ -z "$updated" ] && continue
  mapfile -t sources < <(printf '%s\n' "$fm" | sed -n '/^sources:/,/^[^[:space:]-]/p' | sed -n 's/^[[:space:]]*-[[:space:]]*//p')
  for src in "${sources[@]}"; do
    src="${src%\"}"; src="${src#\"}"
    [ -f "$src" ] || continue   # repo 파일만 추적 (PR#/SHA/memory는 -f 실패→skip)
    last="$(git log -1 --format=%cs -- "$src" 2>/dev/null || true)"
    if [ -n "$last" ] && [[ "$last" > "$updated" ]]; then
      echo "STALE: $page (소스 $src 변경 $last > updated $updated)"
      stale_count=$((stale_count+1))
    fi
  done
done
echo "---"
echo "stale 페이지: $stale_count"
exit 0
```

- [ ] **Step 2: `wiki/scripts/check-unprocessed-docs.sh` 작성**

```bash
#!/usr/bin/env bash
# docs/ 중 어떤 위키 페이지에서도 참조되지 않은 문서(미흡수) 목록.
set -euo pipefail
REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
cd "$REPO_ROOT"
referenced="$(grep -rhoE 'docs/[A-Za-z0-9._/-]+\.md' wiki/ 2>/dev/null | sort -u || true)"
unprocessed=0
while IFS= read -r doc; do
  if ! printf '%s\n' "$referenced" | grep -qxF "$doc"; then
    echo "UNPROCESSED: $doc"
    unprocessed=$((unprocessed+1))
  fi
done < <(find docs -name '*.md' ! -path 'docs/archive/*' ! -path 'docs/superpowers/*' 2>/dev/null)
echo "---"
echo "미흡수 docs: $unprocessed"
exit 0
```

- [ ] **Step 3: Red — stale fixture로 감지 확인**

```bash
chmod +x wiki/scripts/*.sh
mkdir -p wiki/architecture
cat > wiki/architecture/_staletest.md <<'EOF'
---
area: architecture
updated: 2020-01-01
status: current
sources:
  - CLAUDE.md
tags: [test]
---
# stale 테스트
EOF
bash wiki/scripts/check-staleness.sh
```
Expected: `STALE: wiki/architecture/_staletest.md ...` 출력, `stale 페이지: 1` (CLAUDE.md가 2020 이후 변경됐으므로).

- [ ] **Step 4: Green — updated 미래로 → 비감지 확인**

```bash
sed -i 's/updated: 2020-01-01/updated: 2099-01-01/' wiki/architecture/_staletest.md
bash wiki/scripts/check-staleness.sh
```
Expected: `_staletest.md` 미출력, `stale 페이지: 0`.

- [ ] **Step 5: fixture 제거**

```bash
rm wiki/architecture/_staletest.md
```

- [ ] **Step 6: unprocessed-docs 동작 확인**

```bash
bash wiki/scripts/check-unprocessed-docs.sh | tail -3
```
Expected: `미흡수 docs: N` (N>0, 아직 ingest 0건이므로 docs/ 다수가 미흡수로 나옴 — 정상).

- [ ] **Step 7: Commit**

```bash
git add wiki/scripts/
git commit -m "feat(wiki): 최신화 감지 스크립트 — check-staleness / check-unprocessed-docs

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 4: 슬래시 커맨드 3종

**Files:**
- Create: `.claude/commands/ingest.md`, `.claude/commands/query.md`, `.claude/commands/lint.md`

**Interfaces:**
- Consumes: `wiki/AGENTS.md`(규약), Task 3 스크립트.
- Produces: `/ingest` `/query` `/lint` 진입점. 규칙 중복 없이 AGENTS.md 참조.

- [ ] **Step 1: `.claude/commands/ingest.md` 작성**

````markdown
---
description: 원천 소스를 wiki에 정제·반영 (LLM Wiki: Ingest)
---

`$ARGUMENTS`(파일 경로 또는 자료)를 위키에 정제 반영한다.

**먼저 `wiki/AGENTS.md` §5(Ingest 워크플로우)·§3·§4·§8을 읽고 그대로 따른다.**

절차 요약(상세는 AGENTS.md §5):
1. 소스 + AGENTS.md 읽기
2. 핵심 takeaway 1~2줄 사용자와 논의
3. `wiki/sources/<slug>.md` 요약(압축 10~20%, 직접인용 ≤3줄)
4. 영향 architecture/decisions/domain 페이지 갱신(모순 플래그)
5. `wiki/index.md` 갱신 + `wiki/log.md` append

제약: raw(`uniqn-mobile/`·`docs/`·`memory/`) 수정 금지 · frontmatter 5필드 의무 · 무출처 금지 · cross-link ≥3.

출력: 생성/갱신 페이지 경로 · index 항목 · log 엔트리.
````

- [ ] **Step 2: `.claude/commands/query.md` 작성**

````markdown
---
description: wiki 기반 인용 답변 (읽기 전용) - LLM Wiki: Query
---

`$ARGUMENTS`(질문)에 위키 기반으로 답한다.

**먼저 `wiki/AGENTS.md` §6(Query 워크플로우)을 읽고 그대로 따른다.**

절차 요약:
1. `wiki/index.md` → `grep "^## \[" wiki/log.md | tail -5` → AGENTS.md
2. 관련 페이지 ≤7개로 좁힘(백링크 1단계 확장 허용)
3. 인용 답변 합성(주장마다 `[[링크]]`), 데이터 갭 명시

출력 형식:
- **답**: 3~10문장, 모든 주장 `[[링크]]` 인용
- **관련 노트**: 열 순서대로 ≤7개 (각 한줄 요약 + 왜)
- **다음 액션**: 데이터 갭 / 모순 / 환류 제안

제약: raw 직접 읽기 지양(부족 시 명시 후 ≤2개) · 읽기 전용(위키 안 씀) · 추측 금지 · 인용 ≥3(못 채우면 갭 처리).
````

- [ ] **Step 3: `.claude/commands/lint.md` 작성**

````markdown
---
description: wiki 건강 진단 (모순·stale·고립·갭) - LLM Wiki: Lint
---

`$ARGUMENTS`(영역명 또는 "all", 생략 시 all) 위키 건강을 진단한다. **자동 수정 X — 진단만.**

**먼저 `wiki/AGENTS.md` §7(Lint 워크플로우)을 읽고 그대로 따른다.**

절차:
1. `bash wiki/scripts/check-staleness.sh` → stale 페이지
2. `bash wiki/scripts/check-unprocessed-docs.sh` → 미흡수 docs
3. `wiki/index.md` + 전 페이지 frontmatter 스캔 → 모순·고아(백링크≤2)·데이터갭·증거공백
4. `wiki/log.md`에 `## [YYYY-MM-DD] lint | <범위>` append

출력 형식:
- **A. 진단 표**: | 항목 | 개수 | 우선순위 | (stale·미흡수docs·모순·고아·데이터갭·증거공백)
- **B. 우선 조치 top3**
- **C. 추가 탐색 제안**

제약: 진단만(자동수정 X). 조치는 사용자 승인 후 `/ingest` 또는 수동 편집.
````

- [ ] **Step 4: 검증**

Run: `for f in ingest query lint; do echo "== $f =="; head -3 .claude/commands/$f.md; grep -c "AGENTS.md" .claude/commands/$f.md; done`
Expected: 각 파일 frontmatter `description` 존재 + `AGENTS.md` 참조 ≥1.

- [ ] **Step 5: Commit**

```bash
git add .claude/commands/
git commit -m "feat(wiki): 슬래시 커맨드 /ingest /query /lint (AGENTS.md 참조 진입점)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 5: architecture 백본 시드 (layers · data-flow · rls-model)

**Files:**
- Create: `wiki/architecture/layers.md`, `wiki/architecture/data-flow.md`, `wiki/architecture/rls-model.md`

**Interfaces:**
- Consumes: AGENTS.md 규약, 원천 소스(아래 명시).
- Produces: architecture 영역 3페이지. Task 8이 index/overview에 링크.

**필수 소스 (읽고 인용):**
- `layers.md`: `CLAUDE.md` 아키텍처 섹션(`Presentation → Hooks → Service → Repository → Supabase`) + 실제 구조 `uniqn-mobile/src/{services,repositories,hooks,components,features,domains}/`.
- `data-flow.md`: `uniqn-mobile/src/services/` + `uniqn-mobile/src/repositories/` 대표 흐름 1~2개(예: wallet 또는 jobs). TanStack Query 읽기전용 Repository 직접호출 예외(CLAUDE.md) 반영.
- `rls-model.md`: `uniqn-mobile/supabase/migrations/` RLS 정책 + memory `pitfall_anon_rls_secdef_function_poison` · `pitfall_rls_jpc_recursion_widespread` · `pitfall_rls_with_check_self_select_recursion`.

- [ ] **Step 1: `wiki/architecture/layers.md` 작성** (아래 템플릿 구조, 내용은 소스 합성)

각 페이지 공통 형식 — 예시(layers.md):

```markdown
---
area: architecture
updated: 2026-06-18
status: current
sources:
  - CLAUDE.md
  - uniqn-mobile/src/services
  - uniqn-mobile/src/repositories
tags: [architecture, layers]
---

# 레이어 아키텍처

**한 줄:** `Presentation → Hooks → Service → Repository → Supabase` 단방향 의존. (검증됨: CLAUDE.md 아키텍처 섹션)

## 레이어별 책임
- **Presentation** (`uniqn-mobile/src/components`, `features`) — UI. Supabase 직접 호출 금지.
- **Hooks** (`uniqn-mobile/src/hooks`) — 화면 상태·이펙트.
- **Service** (`uniqn-mobile/src/services`) — 비즈니스 로직. DB 접근의 진입점.
- **Repository** (`uniqn-mobile/src/repositories`) — Supabase 쿼리 캡슐화.
- **Supabase** — Auth + PostgreSQL + Realtime.

## 규칙 (검증됨: CLAUDE.md)
- DB 접근: Service → Repository → Supabase 경유 필수.
- 예외: TanStack Query 읽기전용 조회는 Repository 직접 호출 허용. Auth는 authService + 인증 hook만.

## 관련
- [[data-flow]] · [[rls-model]] · [[roles]] (domain)
```

- [ ] **Step 2: `wiki/architecture/data-flow.md` 작성** — 위 형식대로, 대표 흐름(예: wallet 충전/정산 또는 jobs 지원→확정) 1~2개를 `파일:줄` 인용으로. cross-link ≥3(그중 타 영역 ≥1, 예: [[revenue-model]] 또는 [[capacity-full]]).

- [ ] **Step 3: `wiki/architecture/rls-model.md` 작성** — RLS 모델 + 재귀/SECDEF 함정 3건 요약. sources에 migrations 경로 + `PR#`(해당 시). cross-link ≥3(예: [[layers]] · [[roles]] · [[enum-divergence]]).

- [ ] **Step 4: 검증**

Run: `for f in layers data-flow rls-model; do echo "== $f =="; head -12 wiki/architecture/$f.md; grep -c "\[\[" wiki/architecture/$f.md; done`
Expected: 각 파일 frontmatter 5필드 + 본문 `[[링크]]` ≥3.

- [ ] **Step 5: Commit**

```bash
git add wiki/architecture/
git commit -m "feat(wiki): architecture 백본 시드 — layers/data-flow/rls-model

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 6: domain 백본 시드 (roles · target-market · revenue-model)

**Files:**
- Create: `wiki/domain/roles.md`, `wiki/domain/target-market.md`, `wiki/domain/revenue-model.md`

**필수 소스:**
- `roles.md`: `CLAUDE.md` 역할 섹션(`admin > employer > staff`, UserRole(앱권한) ≠ StaffRole(dealer/floor/serving)).
- `target-market.md`: memory `project_target_market_pivot` (홀덤펍 사장 단발알바 + 대회사 운영팀 D-7~D-day. 포커룸 비타깃).
- `revenue-model.md`: memory `project_revenue_model_audit_20260609` (지갑/IAP/RevenueCat, P1 봉쇄, 게이트 OFF 휴면).

- [ ] **Step 1: `wiki/domain/roles.md` 작성** — frontmatter 5필드(sources: `CLAUDE.md`), UserRole vs StaffRole 명확 구분. cross-link ≥3(예: [[rls-model]] · [[layers]] · [[target-market]]).

- [ ] **Step 2: `wiki/domain/target-market.md` 작성** — 타깃=홀덤펍+대회사, 포커룸 비타깃 명시. sources: `memory/project_target_market_pivot.md`(정보성). cross-link ≥3(예: [[revenue-model]] · [[roles]] · [[data-flow]]).

- [ ] **Step 3: `wiki/domain/revenue-model.md` 작성** — 수익모델 현황(지갑·IAP·RC, 출시게이트 OFF 휴면). sources: `memory/project_revenue_model_audit_20260609.md` + `PR#172`. cross-link ≥3(예: [[target-market]] · [[data-flow]] · [[enum-divergence]]).

- [ ] **Step 4: 검증**

Run: `for f in roles target-market revenue-model; do echo "== $f =="; head -12 wiki/domain/$f.md; grep -c "\[\[" wiki/domain/$f.md; done`
Expected: 각 파일 frontmatter 5필드 + `[[링크]]` ≥3.

- [ ] **Step 5: Commit**

```bash
git add wiki/domain/
git commit -m "feat(wiki): domain 백본 시드 — roles/target-market/revenue-model

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 7: decisions 시드 (pitfall 합성, 최소 3건)

**Files:**
- Create: `wiki/decisions/enum-divergence.md`, `wiki/decisions/worktime-ssot.md`, `wiki/decisions/capacity-full.md`

**필수 소스:**
- `enum-divergence.md`: memory `pitfall_enum_divergence_read_disappearance` (+ 관련 `pitfall_fixed_schedule_strict_parse_kills_backcompat`). 교훈: enum 신규값은 status 필터 reader 전수 갱신, 읽기 Zod `.catch()`/`.or()`.
- `worktime-ssot.md`: memory `pitfall_worktime_display_ssot_divergence` (WorkTimeDisplay 우회 → 미정 불일치, PR#170). 교훈: 새 근무시간 화면은 WorkTimeDisplay 경유 필수.
- `capacity-full.md`: memory `project_job_posting_capacity_full_shipped` + `pitfall_posting_role_filled_dead_counter` + `pitfall_e2e_employer_tab_nonemployer_hydration`. 결정: posting_status `capacity_full` enum + 트리거 자동전이 + reader 전수갱신.

- [ ] **Step 1~3: 각 decisions 페이지 작성** — ADR 톤(맥락→결정→결과→교훈). frontmatter sources에 memory 파일(정보성) + 해당 `PR#`. 각 cross-link ≥3(그중 타 영역 ≥1, 예: enum-divergence → [[rls-model]] · [[revenue-model]] · [[capacity-full]]).

decisions 페이지 형식 예시(enum-divergence.md):

```markdown
---
area: decisions
updated: 2026-06-18
status: current
sources:
  - memory/pitfall_enum_divergence_read_disappearance.md
  - PR#146
tags: [enum, read-disappearance, zod]
---

# 결정: enum 발산 → 읽기 레코드 증발 방지

**맥락:** posting_status/payroll_status enum을 앱·DB 두 곳에 수기 복제 → 한쪽에 신규값 생기면 reader status 필터가 누락해 레코드가 "증발"(PR#146/#155/#161 재발 클래스).

**결정:** ① enum 신규값 추가 시 모든 status 필터 reader 전수 갱신 ② 읽기 Zod는 `.catch()`/`.or()`로 미지값 허용 ③ enum SSOT에서 파생.

**결과/교훈:** [[capacity-full]] 도입 시 이 규칙으로 reader 누락을 사전 차단. (주장: 현재 생산자 0건이라 미발동이나 누가 값 쓰면 즉시 발동.)

## 관련
- [[rls-model]] · [[capacity-full]] · [[revenue-model]]
```

- [ ] **Step 4: 검증**

Run: `for f in enum-divergence worktime-ssot capacity-full; do echo "== $f =="; head -12 wiki/decisions/$f.md; grep -c "\[\[" wiki/decisions/$f.md; done`
Expected: 각 파일 frontmatter 5필드 + `[[링크]]` ≥3.

- [ ] **Step 5: Commit**

```bash
git add wiki/decisions/
git commit -m "feat(wiki): decisions 시드 — enum-divergence/worktime-ssot/capacity-full

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 8: index/overview 완성 + 전체 검증

**Files:**
- Modify: `wiki/index.md`, `wiki/overview.md`

**Interfaces:**
- Consumes: Task 5/6/7의 모든 시드 페이지.
- Produces: 완성된 카탈로그 + 시작점. 위키 부트스트랩 종료.

- [ ] **Step 1: `wiki/index.md` 영역별 채우기** — 각 시드 페이지를 `- [[slug]] — 한줄요약` 으로. 예:

```markdown
## architecture
- [[layers]] — Presentation→Hooks→Service→Repository→Supabase 단방향
- [[data-flow]] — 대표 데이터 흐름(서비스→리포지토리→Supabase)
- [[rls-model]] — RLS 정책 + 재귀/SECDEF 함정

## decisions
- [[enum-divergence]] — enum 발산 → 읽기 증발 방지 규칙
- [[worktime-ssot]] — 근무시간 표시 SSOT(WorkTimeDisplay)
- [[capacity-full]] — 공고 자동마감 capacity_full

## domain
- [[roles]] — UserRole(앱권한) vs StaffRole(직무)
- [[target-market]] — 홀덤펍 + 대회사 (포커룸 비타깃)
- [[revenue-model]] — 지갑·IAP·RevenueCat (게이트 OFF 휴면)
```

- [ ] **Step 2: `wiki/overview.md` 합성** — status를 `current`로, 시스템 thesis 3~5문장 + 허브 링크(각 영역 대표 페이지). sources에 주요 근거 추가.

- [ ] **Step 3: staleness self-check (Green)**

Run: `bash wiki/scripts/check-staleness.sh`
Expected: `stale 페이지: 0` (방금 updated=2026-06-18로 생성했으므로).

- [ ] **Step 4: dangling 링크 점검** — index에 적힌 모든 `[[slug]]` 대상 파일 존재 확인.

Run: `for s in layers data-flow rls-model enum-divergence worktime-ssot capacity-full roles target-market revenue-model; do test -f "wiki/architecture/$s.md" -o -f "wiki/decisions/$s.md" -o -f "wiki/domain/$s.md" && echo "OK $s" || echo "MISSING $s"; done`
Expected: 9개 모두 `OK`.

- [ ] **Step 5: 인용 표본 검증** — 각 영역 1페이지씩 골라 sources의 repo 파일 경로가 실재하는지 확인.

Run: `grep -hE "^\s+- (uniqn-mobile|docs|CLAUDE)" wiki/**/*.md | sed 's/^[[:space:]]*-[[:space:]]*//' | sort -u | while read p; do test -e "$p" && echo "OK $p" || echo "MISSING $p"; done`
Expected: repo 경로 모두 `OK` (PR#/memory는 미출력).

- [ ] **Step 6: log append + Commit**

```bash
# log.md에 완료 엔트리 추가 (## [2026-06-18] bootstrap | 백본 시드 완료 — 9 페이지)
git add wiki/index.md wiki/overview.md wiki/log.md
git commit -m "feat(wiki): index/overview 완성 + 부트스트랩 검증(stale 0, 링크 9/9)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Self-Review (계획 작성자용 — 실행 전 1회)

**1. Spec coverage:**
- §2 3레이어 → Task 1/2 ✅ · §3 디렉토리 → Task 1 ✅ · §4 페이지규약 → Global + Task 2 ✅ · §5 스키마 → Task 2 ✅ · §6 운영/슬래시 → Task 4 ✅ · §7 하이브리드 시드 → Task 5/6/7 ✅ · §8 옵시디언 → 기존 볼트(추가작업 없음) ✅ · §9 YAGNI → 범위 준수 ✅ · §10 검증 → Task 3 Red-Green + Task 8 검증 ✅ · §11 관계 → AGENTS.md §9 ✅ · §12 최신화 → Task 3 + AGENTS.md §7 ✅.
- 갭 없음.

**2. Placeholder scan:** index/overview/overview의 `_(Task N에서 채움)_`은 Task 1 스켈레톤의 의도된 임시표식이며 Task 8에서 실제 내용으로 대체됨(명시). 그 외 TODO/TBD 없음.

**3. Type consistency:** 슬러그 일관(layers/data-flow/rls-model/enum-divergence/worktime-ssot/capacity-full/roles/target-market/revenue-model), frontmatter 5필드 전 태스크 동일, 스크립트명 일관(check-staleness.sh/check-unprocessed-docs.sh).
